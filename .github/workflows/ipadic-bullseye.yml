name: Build ipadic-bullseye

on:
  push:
    tags:
      - "ipadic-bullseye/*"
  pull_request:
    paths:
      - ".github/workflows/ipadic-bullseye.yml"
      - "ipadic/bullseye/**"
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4
      - uses: actions/checkout@v7
      - uses: shogo82148/actions-setup-perl@v1
        with:
          perl-version: "5.44"
      - run: |
          ./scripts/build.pl ipadic bullseye

      - name: Publish to DockerHub
        if: github.event_name == 'push'
        env:
          USERNAME: ${{ vars.DOCKERHUB_USERNAME }}
          PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
        run: |
          printenv PASSWORD | docker login --username "$USERNAME" --password-stdin
          ./scripts/publish.pl ipadic bullseye "$USERNAME"
          docker logout

      - name: Publish to GitHub Packages Container registry
        if: github.event_name == 'push'
        env:
          USERNAME: ${{ github.repository_owner }}
          PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          printenv PASSWORD | docker login --username "$USERNAME" --password-stdin ghcr.io
          ./scripts/publish.pl ipadic bullseye ghcr.io/"$USERNAME"
          docker logout
